export enum EUiRoutes {
  Home = "",
  Accordion = "accordion",
  Button = "button",
  Icon = "icon",
  Modal = "modal",
  Typography = "typography",
  Switch = "switch",
  Select = "select",
}

export enum EMainRoutes {
  Ui = "ui",
  Hooks = "hooks",
  Contacts = "contacts",
  Others = "others",
}
